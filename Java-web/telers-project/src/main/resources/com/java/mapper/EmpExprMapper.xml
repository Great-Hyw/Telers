<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.java.mapper.EmpExprMapper">

    <insert id="addEmpExpr">
insert into telers.emp_expr(emp_id, begin, end, company, job) VALUES
        <foreach collection="exprList" item="e" separator="," >
            (#{e.empId},#{e.begin},#{e.end},#{e.company},#{e.job})
        </foreach>
;
    </insert>

    <delete id="daleteEmpExpr">
        delete from telers.emp_expr where emp_id in
        <foreach collection="ids" item="empId" open="(" close=")" separator=",">
            #{empId}
        </foreach>
    </delete>
</mapper>