<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.java.mapper.StudentMapper">
    <select id="select" resultType="com.java.pojo.Stu">
        select c.*,cl.name from telers.student c left join telers.clazz cl on c.clazz_id=cl.id
        <where>
            <if test="name != null">
                and c.name like concat('%',#{name},'%')
            </if>
            <if test="degree!=null">
                and c.degree=#{degree}
            </if>
            <if test="clazzId !=null">
                and c.clazz_id=#{clazzId}
            </if>
        </where>
    </select>
</mapper>